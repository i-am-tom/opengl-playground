name: libraries

on:
  push

permissions:
  contents: read

jobs:
  behaviours:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - uses: actions/cache@v3
      with:
        path: |
          ~/.ghcup
          ~/.cabal/packages
          ~/.cabal/store
          dist-newstyle

        key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/*.cabal') }}-${{ hashFiles('**/cabal.project') }}
        restore-keys: |
          ${{ runner.os }}-build-${{ env.cache-name }}-
          ${{ runner.os }}-build-
          ${{ runner.os }}-

    - name: Install Haskell
      run: |
        sudo apt-get update
        curl --proto '=https' --tlsv1.2 -sSf https://get-ghcup.haskell.org | sh

        ghcup install ghc 8.10.7
        ghcup install cabal 3.6.2.0

        ghcup set ghc 8.10.7
        ghcup set cabal 3.6.2.0

    - name: Dependencies
      run: |
        cabal update
        cabal build behaviours --only-dependencies --enable-tests --enable-benchmarks

    - name: Build
      run: cabal build behaviours --enable-tests

    - name: Test
      run: cabal test behaviours

  wavefront:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - uses: actions/cache@v3
      with:
        path: |
          ~/.ghcup
          ~/.cabal/packages
          ~/.cabal/store
          dist-newstyle

        key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/*.cabal') }}-${{ hashFiles('**/cabal.project') }}
        restore-keys: |
          ${{ runner.os }}-build-${{ env.cache-name }}-
          ${{ runner.os }}-build-
          ${{ runner.os }}-

    - name: Install Haskell
      run: |
        sudo apt-get update
        curl --proto '=https' --tlsv1.2 -sSf https://get-ghcup.haskell.org | sh

        ghcup install ghc 8.10.7
        ghcup install cabal 3.6.2.0

        ghcup set ghc 8.10.7
        ghcup set cabal 3.6.2.0

    - name: Dependencies
      run: |
        cabal update
        cabal build wavefront --only-dependencies --enable-tests --enable-benchmarks

    - name: Build
      run: cabal build wavefront --enable-tests

    - name: Test
      run: cabal test wavefront
